---
const { small = false } = Astro.props;
---
<style lang="scss">
  .item-header {
    margin: .6em 0;

    @media (min-width: 550px), only print {
      display: flex;
    }
  }

  .main {
    // Enable grid on bigger sizes than tablet
    @media (min-width: 550px) {
      float: left;
      width: grid-column-width(9);
    }

    @media (min-width: 550px), only print {
      float: none; // Override to enable flexbox properly
      flex: 2;
      width: auto; // Override to enable flexbox properly
    }
  }

  .side {
    font-family: var(--font-family-sans);
    font-size: var(--h3-font-size-small);
    font-weight: 500;

    @media (min-width: 550px), only print {
      float: none; // Override to enable flexbox properly
      width: auto; // Override to enable flexbox properly
      margin-left: 1em;
    }

    // Enable grid on bigger sizes than tablet
    @media (min-width: 550px) {
      float: left;
      width: grid-column-width(3);
      font-size: var(--h3-font-size);
      text-align: right;
      padding-left: 2em;
    }
  }
</style>

<div class="item-header">
  <div class="main">
    <slot name="main" />
  </div>
  <div class="side">
    <slot name="side" />
  </div>
</div>
