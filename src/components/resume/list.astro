<style lang="scss">

  .list {
    --width: 18em;
    --count: 2;
    --gap: 1.5em;
    columns: var(--width) var(--count);
    column-gap: var(--gap);
    padding-left: 0;
    // Has to be not "inside" but using CSS columns with it doesn't work so yeah
    list-style: circle inside;
  }
</style>

<ul class="list">
  <slot />
</ul>
